# -Vallabh verification relay

This public repository is a **verification relay only** for the private website repository.

- Product source does not belong in this repository.
- A successful verification always names one exact immutable private commit SHA.
- Secrets, authenticated repository URLs, private source, and private diagnostics must never be pasted into commits, issues, workflow output, or uploaded artifacts.
- The private repository remains the sole source of truth.
